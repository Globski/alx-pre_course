include <stdio.h>

int main(void)
{
    char la;
    
    for(la = 'a'; la <= 'z'; la++)
    {
        if(la == 'z')
        {
            putchar(la);
            continue;
        }
        putchar(la);
        putchar(',');
        putchar(' ');
     
    }
    
    return 0;
}
